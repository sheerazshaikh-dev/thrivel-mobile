import{useState}from'react';import{router}from'expo-router';import{Alert,Pressable,ScrollView,StyleSheet,Text,View}from'react-native';import{SafeAreaView}from'react-native-safe-area-context';import{Field,GradientButton,Logo,Muted,OutlineButton,Panel,Screen,Title}from'@/components/ui';import{login}from'@/services/auth';import{useBranding}from'@/context/Branding';
export default function Login(){const b=useBranding();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[loading,setLoading]=useState(false);async function submit(){setLoading(true);try{const u=await login(email,password);if(u.role!=='customer'){Alert.alert('Staff account','Use the web admin panel for staff access.');return}router.replace('/dashboard')}catch(e){Alert.alert('Login failed',e instanceof Error?e.message:'Login failed.')}finally{setLoading(false)}}return <Screen><SafeAreaView style={{flex:1}}><ScrollView contentContainerStyle={s.wrap} keyboardShouldPersistTaps="handled"><View style={s.top}><Logo height={31}/><OutlineButton title="Take assessment" onPress={()=>router.push('/assessment')}/></View><View style={s.copy}><Title size={31}>{b.loginHeadline}</Title><Muted style={{fontSize:15}}>{b.loginSubheadline}</Muted></View><Panel style={{gap:18}}><View><Title size={24}>{b.loginTitle}</Title><Muted style={{marginTop:5}}>{b.loginDescription}</Muted></View><Field label="Email" value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none" placeholder="you@example.com"/><Field label="Password" value={password} onChangeText={setPassword} secureTextEntry/><GradientButton title={loading?'Logging in…':'Log in'} onPress={submit} disabled={loading||!email||!password}/><Text style={s.center}>New to {b.brandName}? <Text style={s.link} onPress={()=>router.push('/assessment')}>Start assessment</Text></Text><Text style={s.small}>Member accounts are created after a paid checkout.</Text></Panel></ScrollView></SafeAreaView></Screen>}
const s=StyleSheet.create({wrap:{padding:18,paddingBottom:40,gap:28},top:{flexDirection:'row',alignItems:'center',justifyContent:'space-between'},copy:{gap:10,paddingTop:14},center:{color:'rgba(255,255,255,.7)',textAlign:'center',fontSize:13},link:{color:'#EC437D',fontWeight:'800'},small:{color:'rgba(255,255,255,.42)',textAlign:'center',fontSize:11}})
